<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBttons_style">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@color/colorAccent</item>
    </style>

    <style name="Code_inputs_style">
        <item name="android:background">@drawable/rounded_dark_border</item>
        <item name="android:layout_height">@dimen/verify_code_dim</item>
        <item name="android:layout_width">@dimen/verify_code_dim</item>
    </style>

</resources>
